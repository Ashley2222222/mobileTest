<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <application
        android:name="com.ashley.mobileTest.App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.mobileTest" >
        <activity
            android:name="com.ashley.mobileTest.ui.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.mobileTest">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.ashley.mobileTest.ui.BookingActivity"
            android:exported="false" />
        <activity
            android:name="com.ashley.mobileTest.ui.BookingListActivity"
            android:exported="false" />
    </application>

</manifest>